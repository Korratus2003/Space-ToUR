using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enemy0 : Enemy
{
    private void Start()
    {
        base.setHealth(50);
        base.setPoints(10);
    }
}
